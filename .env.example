# Ejemplo de variables de entorno para AI-chan (no usar en producción tal cual)
# Rellena con tus claves reales y renombra a .env

# Tamaño de bloque para resúmenes de memoria (por defecto 32 si vacío)
SUMMARY_BLOCK_SIZE=32

# Claves de API unificadas
GEMINI_API_KEY=pon_aqui_tu_clave_gemini
GEMINI_API_KEY_FALLBACK=
OPENAI_API_KEY=pon_aqui_tu_clave_openai
GOOGLE_CLOUD_API_KEY=pon_aqui_tu_clave_google_cloud

# Configuración de audio/voz
# Proveedor principal: openai | gemini
AUDIO_PROVIDER=gemini
# Modo para Gemini: local | google (solo si AUDIO_PROVIDER=gemini)
AUDIO_TTS_MODE=google
# Voz OpenAI por defecto (sage|shimmer|echo|onyx|fable|alloy|ash|coral)
OPENAI_VOICE=sage

DEFAULT_TEXT_MODEL=gemini-2.5-flash
DEFAULT_IMAGE_MODEL=gpt-4.1-mini

# Imágenes (solo usa estas claves; para desktop usa una ruta absoluta o con ~)
# Recomendado para escritorio: $HOME/AI_chan/images  (puedes usar ~/AI_chan/images)
IMAGE_DIR_ANDROID=/storage/emulated/0/Pictures/AI_chan
IMAGE_DIR_IOS=DCIM/AI_chan            # relativa -> se expandirá dentro de Documents si no comienza por /
IMAGE_DIR_DESKTOP=~/AI_chan/images    # usada para Linux/Mac/Windows; ~ se expandirá a $HOME
IMAGE_DIR_WEB=AI_chan                 # nombre lógico (web Storage / memoria)

# Audios (solo usa estas claves; para desktop usa una ruta absoluta o con ~)
# Recomendado para escritorio: $HOME/AI_chan/audio  (puedes usar ~/AI_chan/audio)
AUDIO_DIR_ANDROID=/data/user/0/com.example.ai_chan/cache
AUDIO_DIR_IOS=Library/Caches          # relativa -> se expandirá dentro de Documents si no comienza por /
AUDIO_DIR_DESKTOP=~/AI_chan/audio     # para Linux/Mac/Windows; ~ se expandirá a $HOME
AUDIO_DIR_WEB=AI_chan_audio           # nombre lógico
